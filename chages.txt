i changes Register to register.
added email field to the user schema avoid the duplicate issue.
changed product.user to product.seller.


to implement the search feature well have to use fuzzy search or cosine similaity.
use gemini embeddings api or a cosine simalairty oackge.
then handle it later in a dedicated service folder so the controller is cleaner


Made the request faster by handing cloudinary upload and gemini api call in the background 